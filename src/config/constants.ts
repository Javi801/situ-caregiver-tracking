export const LOCATION_UPDATE_INTERVAL_MINUTES = 15;
export const SHORT_DISTANCE_THRESHOLD_MINUTES = 20;
export const HANDOFF_WINDOW_MINUTES = 15;
export const DELAY_THRESHOLD_MINUTES = 10;

/** Extra minutes added to the ETA when a delay is simulated. */
export const SIMULATED_DELAY_MINUTES = 25;

/** Risk thresholds expressed in minutes of delay over the scheduled ETA. */
export const MEDIUM_RISK_DELAY_MINUTES = 10;
export const HIGH_RISK_DELAY_MINUTES = 20;
